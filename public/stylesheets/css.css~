body{
    font-size:14px;
    font-family: Verdana, Helvetica, Arial;
    background-color:#fff;
    color:#333;
}
body, div, span, object, iframe, h1, h2, h3, h4, h5, h6, p, blockquote, a, code, em, img, q, small, strong, dd, dl, dt, li, ol, ul, fieldset, form, label, table, tbody, tr, th, td, input {
    margin:0;
    padding:0;
}
ul {
    list-style-image:none;
    list-style-position:outside;
    list-style-type:none;
}
a {
    color:#1B97F2;
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}

a img {
    border: none;
}
h2, h2 a{
    color: #E98C08;
    font-size: 18px;
}
.l{
    float:left;
}
.r{
    float:right;
}
hr{
    border:1px dashed #111;
    width:80%;
    text-align:left;
}
#container{
    height:auto;
    min-width:1000px;
}
#right{
    float:left;
    min-height:450px;
    height:auto;
    width:830px;
    margin:5px 0 0 20px;
}
#rightheader{
    height:50px;
    line-height:2em;
}
#rightcontain{
    min-height:370px;
    height:auto;
}
#footer{
    clear:both;
    height:20px;
    border-top:1px solid #eee;
    margin:20px;
    padding:10px
}
#content{
    margin-top:10px;
}

#swleft{
    min-width:150px;
    width:30%;
    min-height:200px;
    height:auto;
}
#swright{
    min-width:150px;
    width:30%;
    min-height:200px;
    height:auto;
}
#swmid{
    min-width:300px;
    width:40%;
    height:auto;
}
#fstatus{
    height:200px;
    overflow: auto;
    width:90%
}
#cselect{
    min-height:100px;
    height:30%
}

#notice{
    /*  border:#1B97F2 2px solid; */
    margin:10px;
    color:red;
    text-align:center;
    padding:10px;
}

.notice {
    background:transparent url(/images/notice_bg.gif) repeat-x scroll left top;
    border:1px solid #E6D08C;
    font-size:12px;
    height:35px !important;
    line-height:35px !important;
    margin-bottom:10px;
    overflow:hidden;
    padding:0 30px 0 20px;
    position:relative;
    text-align:left;
    width:728px;
}
a.notice_close {
    display:inline-block;
    height:14px;
    left:700px;
    position:absolute;
    top:1px;
    width:120px;
}

#sidebar {
    margin:20px 0 20px 10px;
    background-color:#FCFCF3;
    border-left:1px solid #E5E3D8;
    border-right:1px solid #E5E3D8;
    float:left;
    width:132px;
}
#sidebar .sidebar-top {
    background:transparent url(/images/sidebar/v2_l_top.gif) no-repeat scroll left top;
    height:50px;
    margin:-9px -1px -50px;
}
#sidebar .sidebar-bottom{
    border:0 none;
    background:transparent url(/images/sidebar/v2_bottom.png) no-repeat scroll left top;
    height:10px;
}

#sidebar .app-list {
    padding-top:10px;
}

#sidebar .app-list ul.activelist {
    padding-top:5px;
}
#sidebar .app-list li a {
    background:transparent url() no-repeat scroll 10px 9px;
    display:block;
    font-size:14px;
    font-weight:bold;
    height:30px;
    line-height:30px;
    overflow:hidden;
    padding:0 5px 0 30px;
}

#sidebar .app-list .empty{
    background:transparent url() no-repeat scroll 10px 9px;
    display:block;
    font-size:14px;
    font-weight:bold;
    color:#E98C08;
    height:30px;
    line-height:30px;
    overflow:hidden;
    padding:0 5px 0 30px;
}

#sidebar .app-list li a:hover {
    background-color:#FFFF8C;
}
#sidebar .app-list .border-b {
    border-bottom:1px solid #CCCCCC;
    padding-bottom:5px;
}
#sidebar .app-list li.clipline {
    border-bottom:1px solid #FFFFFF;
    border-top:1px solid #CCCCCC;
    font-size:0;
    height:0;
    line-height:0;
    margin:5px 10px;
}

/*
090529 add
*/
div#chat_button {
    text-align: right;
    position: fixed;
    bottom: 0px;
    right: 10px;
    width: 250px;
    border: 1px solid #ccc;
    padding: 2px;
    vertical-align: middle;
    background-color: #eee;
}
div#chat_input {
    position: fixed;
    bottom: 25px;
    right: 10px;
    text-align: left;
}
div#chat_button img {
    vertical-align: middle;
}
div#chat_button form {
    float:left;
}
#ddchat_show{
    text-align:left;
    max-height:200px;
    max-width:260px;
    overflow:auto;
}
#ddchat_submit{
    background:transparent url(/images/chat/chat.gif) no-repeat top left;
    border:none;width:20px;height:20px;
}

* html div#chat_button {
    position: absolute;
    right: auto; bottom: auto;
    left: expression( ( 0 - chat_button.offsetWidth + ( document.documentElement.clientWidth ? document.documentElement.clientWidth : document.body.clientWidth ) + ( ignoreMe2 = document.documentElement.scrollLeft ? document.documentElement.scrollLeft : document.body.scrollLeft ) ) + 'px' );
    top: expression( ( 0 - chat_button.offsetHeight + ( document.documentElement.clientHeight ? document.documentElement.clientHeight : document.body.clientHeight ) + ( ignoreMe = document.documentElement.scrollTop ? document.documentElement.scrollTop : document.body.scrollTop ) ) + 'px' );
}
* html div#chat_input {
    position: absolute;
    right: auto; bottom: auto;
    left: expression( ( 0 - chat_input.offsetWidth + ( document.documentElement.clientWidth ? document.documentElement.clientWidth : document.body.clientWidth ) + ( ignoreMe2 = document.documentElement.scrollLeft ? document.documentElement.scrollLeft : document.body.scrollLeft ) ) + 'px' );
    top: expression( ( -25 - chat_input.offsetHeight + ( document.documentElement.clientHeight ? document.documentElement.clientHeight : document.body.clientHeight ) + ( ignoreMe = document.documentElement.scrollTop ? document.documentElement.scrollTop : document.body.scrollTop ) ) + 'px' );
}
* html div#chat_input form {
    float:left;
}

/*
    090920 add
*/
li{
    list-style:none
}

